# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# LaTeX build artefacts (keep report.tex and report.pdf)
reports/*.aux
reports/*.log
reports/*.out
reports/*.toc
reports/*.fls
reports/*.fdb_latexmk
reports/*.synctex.gz
reports/*.bbl
reports/*.blg

# XTTS chunk intermediates — regenerated on every run
outputs/_xtts_chunk_*.wav

# Pre-final XTTS / prosody / CMVN intermediates — regenerated by pipeline
outputs/output_LRL_xtts.wav
outputs/output_LRL_flat.wav
outputs/output_LRL_cloned_prewarped.wav
outputs/output_LRL_cloned_cmvn.wav
outputs/parallel_speaker_ref.wav
outputs/lid_pred.json
outputs/lid_gold.npy

# Scratch source audio (large; only original_segment.wav is the submission cut)
data/lecture_full.wav
data/hinglish_full.wav
data/modi_60s_ref_old.wav

# Cached translation / ASR artefacts
outputs/translate_cache.json

# Model caches (HuggingFace, SpeechBrain, Coqui) downloaded at runtime
models/spkrec_ecapa/
models/xtts*/
.cache/

# Source media (large, user-supplied)
*.mp4
data/*.mp4
